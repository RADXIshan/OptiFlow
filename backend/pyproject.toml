[project]
name = "backend"
version = "0.1.0"
description = "OptiFlow Backend API"
readme = "README.md"
requires-python = ">=3.11.9"
dependencies = [
    "fastapi>=0.129.0",
    "python-dotenv>=1.2.1",
    "uvicorn>=0.40.0",
]
